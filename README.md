# waste-category-classification
A Python waste classification program that accepts a waste item as input, converts it to lowercase, checks it against predefined organic, recyclable, and general waste lists, and uses if-elif-else conditions to identify the waste category and display a suitable disposal suggestion.
